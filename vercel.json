{
  "functions": {
    "api/search.js": {
      "maxDuration": 10
    }
  },
  "regions": ["lhr1"],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ],
  "buildCommand": "vite build",
  "outputDirectory": "dist"
}
