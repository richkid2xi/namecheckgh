{
  "functions": {
    "api/search.js": {
      "maxDuration": 10
    }
  },
  "regions": ["lhr"],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ],
  "buildCommand": "vite build",
  "outputDirectory": "dist"
}
